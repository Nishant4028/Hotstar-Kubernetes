Ingress setup
			
az aks show --resource-group terraform-aks-dev --name terraform-aks-dev-cluster --query nodeResourceGroup -o tsv

terraform-aks-dev-nrg


az network public-ip create --resource-group terraform-aks-dev-nrg --name myAKSPublicIPForIngress --sku Standard --allocation-method static --query publicIp.ipAddress -o tsv


172.172.106.96


install helm>>
 
 winget install Helm.Helm

kubectl create namespace ingress-basic


# Add the official stable repository
helm repo add ingress-nginx https://kubernetes.github.io/ingress-nginx
helm repo update


helm show values ingress-nginx/ingress-nginx


	
	
helm install ingress-nginx ingress-nginx/ingress-nginx --namespace ingress-basic \
    --set controller.replicaCount=2 \
    --set controller.nodeSelector."kubernetes\.io/os"=linux \
    --set defaultBackend.nodeSelector."kubernetes\.io/os"=linux \
    --set controller.service.externalTrafficPolicy=Local \
    --set controller.service.loadBalancerIP="172.172.106.96"


create azure dns 
ad nishantg.shop >> add azure namespaces


Create External DNS Manifests

az account show --query "tenantId"     >> "sssssssssssssssssssssssssssss38-853b-2232500bf20b"
# To get Azure Subscription ID
az account show --query "id"     >> "ab86adqqqqqqqqqqqqqqqqqqqqqqqqqqqq"

create azure.json file >> created in folder
create external-dns.yml

located at > D:\Nishant\Leaning\K8s\AKS\AksUdemy\azure-aks-kubernetes-masterclass-master\12-ExternalDNS-for-AzureDNS-on-AKS\kube-manifests\01-ExternalDNS  


Create MSI - Managed Service Identity for External DNS to access Azure DNS Zones
Create Manged Service Identity (MSI)
> Managed Identities  > add 
Resource Name: aksdemo1-externaldns-access-to-dnszones
Resource group: terraform-aks-dev
Location: eastus2


Add Azure Role Assignment in MSI
Opem MSI -> aksdemo1-externaldns-access-to-dnszones
Click on Azure Role Assignments -> Add role assignment
Scope: Resource group
Subscription: Pay-as-you-go
Resource group: dns-zones
Role: Contributor



Make a note of Client Id and update in azure.json
Go to Overview -> Make a note of **Client ID"
Update in azure.json value for userAssignedIdentityID  >> 

Associate MSI in AKS Cluster VMSS
Go to All Services -> Virtual Machine Scale Sets (VMSS) -> Open aksdemo1 related VMSS (aks-agentpool-27193923-vmss)
Go to Settings -> Identity -> User assigned -> Add -> aksdemo1-externaldns-access-to-dnszones


Create Kubernetes Secret and Deploy ExternalDNS
run above to files

go  to location:
kubectl create secret generic azure-config-file --from-file=azure.json

kubectl apply -f external-dns.yml

verify logs> kubectl logs -f $(kubectl get po | egrep -o 'external-dns[A-Za-z0-9-]+')



Implement SSL using Lets Encrypt
Install Cert Manager


kubectl label namespace ingress-basic cert-manager.io/disable-validation=true

helm repo add jetstack https://charts.jetstack.io

# Install the cert-manager Helm chart
helm install \
cert-manager jetstack/cert-manager \
--namespace ingress-basic \
--version v1.13.3 \
--set installCRDs=true


Review or Create Cluster Issuer Kubernetes Manifest


kubectl apply -f kube-manifests/01-CertManager-ClusterIssuer/cluster-issuer.yml

# List Cluster Issuer
kubectl get clusterissuer

# Describe Cluster Issuer
kubectl describe clusterissuer letsencrypt


Review k8s Application Manifests and ingress and deploy
deploy application manifest files and ingree files

